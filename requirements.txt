# Core ML
torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0
peft>=0.10.0
accelerate>=0.28.0
evaluate>=0.4.1
jiwer>=3.0.3

# Audio
soundfile>=0.12.1
librosa>=0.10.1
audiomentations>=0.35.0
# NOTE: we use edge-tts (free Microsoft Edge neural voices, no API key)
# instead of Coqui/XTTS. XTTS pulls in an older transformers internal API
# that conflicts with the modern transformers version needed for Whisper
# fine-tuning below -- installing both in one environment breaks one of
# them. edge-tts has no ML framework dependency at all, so no conflict.
edge-tts>=6.1.0

# Serving
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
streamlit>=1.33.0
requests>=2.31.0
